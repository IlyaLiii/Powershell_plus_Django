Замечания и рекомендации отдела разработки

скрипт
    передавать имя файла, а не main.xml

    1)вынести в начало скрипта url отправки, чтобы проще было поправить, а ещё лучше тоже считывать и проверить выход при невалидном пути или url
    2)скрипт работает только с первым файлом, может сделать на несколько?(отдельными запросами каждый)
    3)после отправки, файл остается на месте, из-за чего он отправиться повторно, может переместить в пупку sended?

сервер
    с файлом записывать дату приема, чтобы отличить одинаковые файлы, если такие будут(либо отправлять дату создания файла и её подписывать)